/* export type RootState = ReturnType<typeof rootReducer>;

export const selectCartModule = (state: RootState) => state.cartApi;

export const selectCartExist = (state: RootState) =>
	selectCartModule(state).total != 0; */
